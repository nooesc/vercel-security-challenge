{
  "name": "sbx044-wildcard-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json && wrangler deploy --dry-run --env allowed && wrangler deploy --dry-run --env denied"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "wrangler": "4.124.0"
  }
}
